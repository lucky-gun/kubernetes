apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: domain-cert
  namespace: cert-manager

spec:
  secretName: domain-tls

  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer

  dnsNames:
  - "*.lucky-gun.com"
