pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Timeout Test') {
            steps {
                retry(3) {
                    timeout(time: 30, unit: 'SECONDS') {
                        sh 'sleep 5'
                    }
                }
            }
        }

        stage('Approval') {
            steps {
                input 'Deploy 진행할까요?'
                echo 'Approved'
            }
        }
    }

    post {
        success {
            echo 'BUILD SUCCESS'
        }

        failure {
            echo 'BUILD FAILURE'
        }
        
        always {
            echo 'ALWAYS RUN'
        }

    }
}
