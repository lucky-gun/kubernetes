kubectl create secret docker-registry regcred \
  --docker-server=https://index.docker.io/v1/ \
  --docker-username= \
  --docker-password= \
  --docker-email= \
  -n infra \
  --dry-run=client -o yaml > registry-secret.yaml


$wgMainCacheType    = CACHE_ACCEL;   // APCu
$wgMessageCacheType = CACHE_ACCEL;   // APCu

$wgSessionCacheType = CACHE_DB;      // ★로그인 쿠키/세션 안정 핵심
$wgParserCacheType  = CACHE_DB; 
MYSQL_HOST=mariadb-galera.database.svc.cluster.local
MYSQL_USER=wiki_admin
MYSQL_PASSWORD=wiki_pass
MYSQL_DATABASE=mediawiki_db

$wgDefaultSkin = 'vector'; 
