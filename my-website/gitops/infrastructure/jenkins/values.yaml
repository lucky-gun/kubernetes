# values.yaml
controller:
  serviceType: ClusterIP

  jenkinsUrl: "https://jenkins.lucky-gun.com"

  admin:
    createSecret: false

  installLatestPlugins: false
  installLatestSpecifiedPlugins: false
  initializeOnce: true

  additionalPlugins:
    - github
    - github-branch-source
    - credentials-binding
    - pipeline-stage-view
    - http_request
    - blueocean

persistence:
  enabled: true
  storageClass: "rook-rbd-sc"
  accessMode: "ReadWriteOnce"
  size: "5Gi"

