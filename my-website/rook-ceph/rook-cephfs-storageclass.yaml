apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: rook-cephfs-sc
provisioner: rook-ceph.cephfs.csi.ceph.com
parameters:
  # Rook-Ceph 클러스터 정보
  clusterID: rook-ceph
  # 위에서 생성한 CephFilesystem 이름과 일치해야 함
  fsName: rook-cephfs
  # 데이터가 저장될 풀 이름 (보통 fsName-data0 형식)
  pool: rook-cephfs-data0

  # CSI 보안 설정 (기존 RBD 설정과 동일한 네임스페이스 참조)
  csi.storage.k8s.io/provisioner-secret-name: rook-csi-cephfs-provisioner
  csi.storage.k8s.io/provisioner-secret-namespace: rook-ceph
  csi.storage.k8s.io/node-stage-secret-name: rook-csi-cephfs-node
  csi.storage.k8s.io/node-stage-secret-namespace: rook-ceph

# 효율적인 운영을 위한 정책 설정
reclaimPolicy: Delete        # PVC 삭제 시 데이터 함께 삭제 (학습용 권장)
allowVolumeExpansion: true   # 필요 시 용량 증설 가능
volumeBindingMode: Immediate
